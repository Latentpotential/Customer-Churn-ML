# Model-load + inference deps for the serving image (pass 1 of 2).
# Pinned to match the model logged by MLflow (artifacts/requirements.txt) so the
# pickled scikit-learn model unpickles cleanly. All pins have cp312 wheels — keep
# the base image at python:3.12-slim so no compiler is needed.
#
# The web stack (FastAPI/Gradio) is installed separately in requirements-web.txt.
# Reason: mlflow-skinny 3.12.0 caps starlette<1 while gradio 6 needs starlette>=1,
# so they cannot co-resolve in one pip pass. They DO coexist at runtime (mlflow
# only uses starlette for its tracking server, which we don't run here), exactly
# as in the project's dev environment. The two-pass install reproduces that state.
mlflow==3.12.0
cloudpickle==3.1.2
numpy==1.26.4
pandas==2.3.3
pyarrow==23.0.1
scikit-learn==1.8.0
scipy==1.17.1
xgboost==3.2.0
psutil==7.2.2
